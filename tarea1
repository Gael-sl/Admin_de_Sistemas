#!/bin/bash
echo "=== Estado del sistema ==="
echo "Hostname: $(hostname)"
echo "IP red interna:"
ip addr show | grep "inet 192.168.100" | awk '{print $2}' | cut -d/ -f1
echo "Espacio en disco:"
df -h /
